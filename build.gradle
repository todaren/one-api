import io.qameta.allure.gradle.task.AllureReport

plugins {
    id 'java'
    id 'io.qameta.allure' version '2.7.0'
}

group 'com.example.one-api'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.konghq:unirest-java:3.3.00'
    testCompile group: 'junit', name: 'junit', version: '4.12'
    testCompile group: 'org.hamcrest', name: 'hamcrest-all', version: '1.3'
}

test {
    useJUnit()
}

allure {
    autoconfigure = false
    version = '2.13.1'
    resultsDir = file("$projectDir/build/test-results/test")
    reportDir = file('allure')
    downloadLink = "file:///$projectDir/.allure/allure-2.13.1.zip"

    useJUnit4 {
        version = '2.13.1'
    }
}

task generateAllure(type: AllureReport) {
}